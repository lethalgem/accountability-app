{
  "name": "accountability-app",
  "version": "1.0.0",
  "description": "Couples accountability app with financial penalty tracking",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "init-db": "wrangler d1 execute accountability-app --remote --file=./schema.sql",
    "init-db:local": "wrangler d1 execute accountability-app --local --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.3.3",
    "wrangler": "^4.0.0"
  }
}
